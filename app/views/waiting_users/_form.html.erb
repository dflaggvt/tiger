<%= form_for(@waiting_user) do |f| %>
  <% if @waiting_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@waiting_user.errors.count, "error") %> prohibited this waiting_user from being saved:</h2>

      <ul>
      <% @waiting_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :emailaddress %><br>
    <%= f.text_field :emailaddress %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
